# The analytical CSV remains DVC-tracked for checksum-based versioning.
# No shared remote is committed: independent reproduction uses the public INEI
# download route in data/download_endes.py, without credentials.
